<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.sgrimailo.cards">

    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CardSetDetailsActivity" />
        <activity android:name=".CardSetCardsActivity" />
        <activity android:name=".CardDetailsActivity" />
        <activity android:name=".CardSetsListActivity" />
        <activity android:name=".CardsPlayerActivity" />
        <activity android:name="com.gmail.sgrimailo.utils.file_explorer.FileExplorerActivity" />
    </application>

</manifest>